apiVersion: batch/v1
kind: Job
metadata:
  name: demo-job
spec:
  template:
    spec:
      containers:
        - name: demo-job-container
          image: alpine
          command: ["/bin/sh"] 
          args: ["-c", "echo Hello Kubernetes! && sleep 30"]
      restartPolicy: Never